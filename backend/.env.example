# Server Port
PORT=5000

# Database Configuration
# For SQLite (default local dev):
SQLITE_PATH=../newspulse.db

# For PostgreSQL / Supabase / Neon in production:
# DATABASE_URL=postgresql://user:password@host:5432/dbname?sslmode=require

# Python Binary (optional override if using custom venv)
PYTHON_BIN=python
